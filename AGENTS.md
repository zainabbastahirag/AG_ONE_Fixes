## Cursor Cloud specific instructions

This repository is currently a blank scaffold with no application code, dependencies, or services.

- The only file is `TEST.TEXT` (a placeholder with content "ASD").
- There are no build systems, package managers, frameworks, or configuration files.
- No lint, test, build, or run commands are applicable.
- When application code is added, update this section with relevant dev environment instructions.
